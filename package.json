{
  "name": "vsm-quiz-app",
  "version": "1.0.0",
  "description": "Quiz app for VSM 2025",
  "private": true,
  "scripts": {
    "build": "echo 'No client build step needed'"
  },
  "dependencies": {
    "@google/genai": "^1.9.0"
  }
}
